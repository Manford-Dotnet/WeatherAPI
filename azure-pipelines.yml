# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- main

pool:
  name: 'Default'

steps:

- task: UseDotNet@2
  displayName: 'Install .NET SDK'
  inputs:
    packageType: 'sdk'
    version: '8.0.404'

- task: DotNetCoreCLI@2
  displayName: 'Restore'
  inputs:
    command: 'restore'
    projects: '**/*.csproj'

- task: DotNetCoreCLI@2
  displayName: 'Build'
  inputs:
    command: 'build'
    projects: '**/*.csproj'
    arguments: '--configuration Release'

- task: DotNetCoreCLI@2
  displayName: 'Test'
  inputs:
    command: 'test'
    projects: '**/*Tests.csproj'
    arguments: '--configuration Release'

- powershell: |
    Import-Module WebAdministration
    Stop-WebSite -Name "WeatherAPI"
  displayName: 'Stop IIS Site'

- powershell: |
    dotnet publish "$(Build.SourcesDirectory)\GetWeatherAPI.csproj" -c Release -o "C:\inetpub\wwwroot\WeatherAPI"
  displayName: 'Publish To IIS'

- powershell: |
    Import-Module WebAdministration
    Start-WebSite -Name "WeatherAPI"
  displayName: 'Start IIS Site'